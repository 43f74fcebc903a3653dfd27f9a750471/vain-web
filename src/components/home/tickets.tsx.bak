"use client";

import { useState } from "react";
import { motion, AnimatePresence } from "framer-motion";
import { herov2Item, heroContainer } from "@/components/ui/animations";
import { gradientText, cardStyles } from "@/components/ui/styles";
import { BiSolidMessageSquareDetail } from "react-icons/bi";
import { Trash2 } from "lucide-react";
import Image from "next/image";
import check from "@/assets/emojis/check.png";

export default function TicketsSection() {
  const [isAnimatingClose, setIsAnimatingClose] = useState(false);
  const [isClosing, setIsClosing] = useState(false);
  const randomId = Math.random().toString(36).substring(2, 10);

  return (
    <motion.div
      variants={heroContainer}
      initial="hidden"
      whileInView="show"
      viewport={{ once: true, amount: 0.3 }}
      className="flex flex-col lg:flex-row gap-16 items-center mb-24"
    >
      <motion.div variants={herov2Item} className="flex-1 space-y-6">
        <div className="flex items-center gap-3 mb-6">
          <div className={cardStyles.icon}>
            <BiSolidMessageSquareDetail
              className="w-5 h-5 text-vain-primary"
              strokeWidth={1}
            />
          </div>
          <span className="text-sm font-medium text-vain-primary/80">
            Support System
          </span>
        </div>
        <motion.h2
          variants={herov2Item}
          className={`text-4xl md:text-5xl font-medium ${gradientText.primary.className}`}
          style={gradientText.primary.style}
        >
          Professional tickets
        </motion.h2>
        <motion.p
          variants={herov2Item}
          className="text-lg text-white/60 max-w-2xl -tracking-wide"
        >
          Handle support requests efficiently with organized tickets. Includes
          auto-transcripts and staff management for better community support.
        </motion.p>
      </motion.div>
      <motion.div variants={herov2Item} className="flex-1">
        <div className="space-y-3">
          <motion.div
            layout
            transition={{
              layout: {
                duration: 0.65,
                type: "spring",
                bounce: 0.22,
                damping: 18,
              },
            }}
            className="group relative bg-gradient-to-bl from-vain-500/35 to-black/70 rounded-2xl p-3.5 
            shadow-[inset_0_1px_1px_rgba(255,255,255,0.1),_0_1px_3px_rgba(0,0,0,0.3),_inset_0_-1px_1px_rgba(0,0,0,0.2)] 
            before:absolute before:inset-0 before:rounded-2xl before:bg-gradient-to-b before:from-white/[0.02] before:to-transparent before:opacity-0
            before:transition-opacity before:-z-10
            after:absolute after:inset-0 after:rounded-2xl after:bg-gradient-to-t after:from-black/30 after:to-transparent after:z-[-1]
            border border-white/[0.03] backdrop-blur-sm
            transition-all duration-500"
            style={{
              willChange: "opacity, transform, filter",
              backdropFilter: "blur(6px)",
              WebkitBackdropFilter: "blur(6px)",
            }}
          >
            <div className="flex items-start gap-3">
              <Image
                src="https://r2.vain.bot/vain-marketing.png"
                alt="vain's avatar"
                width={40}
                height={40}
                className="w-10 h-10 rounded-full"
                style={{
                  willChange: "filter",
                  filter: "blur(0.5px)",
                }}
              />
              <div className="flex-1">
                <div className="flex items-center gap-2 mb-1">
                  <span className="text-white font-medium text-sm">vain</span>
                  <span className="inline-flex items-center bg-vain-primary/50 rounded px-1 h-4 text-white text-[0.625rem] font-semibold uppercase relative">
                    <svg
                      className="w-3 h-3 mr-1"
                      viewBox="0 0 24 24"
                      fill="currentColor"
                    >
                      <path
                        fillRule="evenodd"
                        d="M19.06 6.94a1.5 1.5 0 0 1 0 2.12l-8 8a1.5 1.5 0 0 1-2.12 0l-4-4a1.5 1.5 0 0 1 2.12-2.12L10 13.88l6.94-6.94a1.5 1.5 0 0 1 2.12 0Z"
                        clipRule="evenodd"
                      />
                    </svg>
                    APP
                  </span>
                  <span className="text-white/50 text-xs">Today at 00:25</span>
                </div>
                <AnimatePresence mode="wait">
                  {!isClosing ? (
                    <motion.div
                      key="initial"
                      layout
                      initial={{
                        opacity: 1,
                        y: 0,
                        filter: "blur(2px)",
                      }}
                      animate={{
                        opacity: 1,
                        y: 0,
                        filter: "blur(0px)",
                      }}
                      exit={{
                        opacity: 0,
                        y: -10,
                        filter: "blur(3px)",
                        transition: {
                          duration: 0.28,
                          ease: [0.4, 0.0, 0.2, 1],
                        },
                      }}
                      transition={{
                        layout: {
                          duration: 0.65,
                          type: "spring",
                          bounce: 0.22,
                          damping: 18,
                        },
                        filter: {
                          duration: 0.38,
                          ease: [0.4, 0.0, 0.2, 1],
                        },
                        opacity: { duration: 0.32 },
                      }}
                      style={{
                        willChange: "opacity, transform, filter",
                        backdropFilter: "blur(0px)",
                        WebkitBackdropFilter: "blur(0px)",
                      }}
                    >
                      <div className="text-white/90 text-sm mb-1">
                        Welcome{" "}
                        <span className="text-vain-primary bg-vain-primary/20 px-1 py-0.5 rounded text-xs">
                          @odecy
                        </span>
                      </div>
                      <motion.div
                        layout
                        initial={{ filter: "blur(2px)" }}
                        animate={{ filter: "blur(0px)" }}
                        transition={{
                          filter: {
                            duration: 0.38,
                            ease: [0.4, 0.0, 0.2, 1],
                          },
                        }}
                        style={{
                          willChange: "filter",
                        }}
                      >
                        <div className="bg-white/[0.06] rounded-lg p-3 border-l-4 border-vain-primary mb-1">
                          <div className="flex items-center gap-2 mb-1">
                            <Image
                              src="https://r2.vain.bot/vain-marketing.png"
                              alt="Server icon"
                              width={24}
                              height={24}
                              className="w-5 h-5 rounded-full"
                              style={{
                                willChange: "filter",
                                filter: "blur(0px)",
                              }}
                            />
                            <span className="text-white text-sm font-medium">
                              vain
                            </span>
                          </div>
                          <div className="text-white/90 text-sm mb-2">
                            Be patient and staff will be right with you.
                          </div>
                          <div className="flex items-center gap-2 text-xs text-white/50">
                            <span>vain.bot • Today at 00:25</span>
                          </div>
                        </div>
                      </motion.div>
                      <motion.button
                        onClick={() => {
                          setIsAnimatingClose(true);
                          setTimeout(
                            () => {
                              setIsClosing(true);
                              setTimeout(() => {
                                setIsAnimatingClose(false);
                                setIsClosing(false);
                              }, 5000);
                            },
                            Math.random() * 1000 + 900
                          );
                        }}
                        disabled={isAnimatingClose}
                        className="bg-red-500/20 hover:bg-red-500/30 disabled:bg-red-500/10 text-red-400 px-3 py-1.5 rounded-lg text-sm font-medium flex items-center gap-2 transition-colors border border-red-500/30 disabled:cursor-not-allowed disabled:opacity-70"
                        whileTap={{ scale: 0.97 }}
                        layout
                        transition={{
                          layout: {
                            duration: 0.45,
                            type: "spring",
                            bounce: 0.18,
                          },
                        }}
                        style={{
                          willChange: "transform, filter",
                          filter: isAnimatingClose
                            ? "blur(0.5px)"
                            : "blur(0px)",
                        }}
                      >
                        {isAnimatingClose ? (
                          <>
                            <div className="w-4 h-4 border-2 border-t-transparent border-red-400 rounded-full animate-spin" />
                            Closing...
                          </>
                        ) : (
                          <>
                            <Trash2 className="w-4 h-4" strokeWidth={1} />
                            Close Ticket
                          </>
                        )}
                      </motion.button>
                    </motion.div>
                  ) : (
                    <motion.div
                      key="closing"
                      layout
                      initial={{
                        opacity: 0,
                        y: 10,
                        filter: "blur(3px)",
                      }}
                      animate={{
                        opacity: 1,
                        y: 0,
                        filter: "blur(0px)",
                      }}
                      exit={{
                        opacity: 0,
                        y: -5,
                        filter: "blur(3px)",
                        transition: {
                          duration: 0.5,
                          ease: [0.4, 0.0, 0.2, 1],
                        },
                      }}
                      transition={{
                        duration: 0.45,
                        ease: [0.4, 0.0, 0.2, 1],
                        layout: {
                          duration: 0.65,
                          type: "spring",
                          bounce: 0.22,
                          damping: 18,
                        },
                        filter: {
                          duration: 0.38,
                          ease: [0.4, 0.0, 0.2, 1],
                        },
                        opacity: { duration: 0.32 },
                      }}
                      style={{
                        willChange: "opacity, transform, filter",
                        backdropFilter: "blur(0px)",
                        WebkitBackdropFilter: "blur(0px)",
                      }}
                    >
                      <motion.div layout>
                        <div className="bg-white/[0.06] rounded-lg p-3 border-l-4 border-vain-primary mb-1">
                          <div className="flex items-start gap-2">
                            <img
                              src={check.src}
                              alt="approve"
                              className="inline object-contain w-5 h-5 mt-0.5 flex-shrink-0"
                              style={{
                                willChange: "filter",
                                filter: "blur(0px)",
                              }}
                            />
                            <div className="text-white/90 text-sm">
                              <span className="text-vain-primary bg-vain-primary/20 px-1 py-0.5 rounded text-xs">
                                @odecy
                              </span>
                              : Your logs can be found here:{" "}
                              <a
                                href={`https://vain.bot/tickets/${randomId}`}
                                className="text-vain-primary hover:underline"
                              >
                                https://vain.bot/tickets/{randomId}
                              </a>
                            </div>
                          </div>
                        </div>
                      </motion.div>
                    </motion.div>
                  )}
                </AnimatePresence>
              </div>
            </div>
          </motion.div>
        </div>
      </motion.div>
    </motion.div>
  );
}
